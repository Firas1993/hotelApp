import React from 'react';

import Hero from '../component/Hero';

const Rooms = () => {
    return (
        <div>
            <Hero hero="roomsHero" />
        </div>
    );
}

export default Rooms;
